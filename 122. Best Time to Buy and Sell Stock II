/* Question: find the max profit when trade is no limit
1. sum up all the profit if positive
*/

// Time Complexity: O(n), Space Complexity: O(1)
// Sol: simple case for check every day price and compare
class Solution {
public:
    int maxProfit(vector<int>& prices) {
        int sum=0;

        for(int i=1;i<prices.size();i++){
            if(prices[i]>prices[i-1])
                sum+=prices[i]-prices[i-1];
        }

        return sum;
    }
};


// class Solution {
// public:
//     int maxProfit(vector<int>& prices) {
//         int hold = -prices[0], sum = 0;
//         for(int i=1; i<prices.size(); i++){
//             int sell = hold + prices[i];
//             if(sell > 0){
//                 sum += sell;
//                 hold = -prices[i];
//             }
//             hold = max(hold, -prices[i]);
//         }
//         return sum;
//     }
// };
