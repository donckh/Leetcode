//Time Complexity: O(n), Space Complexity: O(1)
class Solution {
public:
    void reverseString(vector<char>& s) {
        int len = s.size()-1, i=0;
        while(i<len){
            swap(s[i++], s[len--]);
        }
    }
};
